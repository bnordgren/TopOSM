
* stats improvements
   (todo) output lengths of important and missing queues.

* queuemaster commands
   (todo) quit - Should stop consuming expirations and then commit all
            current expirations to disk before actually exiting.

* Tile proxy.

